@page "{id:int}"
@model NorthwindTradersV8WebRazorPages.Pages.Ventas.Editar2Model
@{
    ViewData["Title"] = "Editar venta";
}
<h2>@ViewData["Title"]</h2>
@*
@if (!ViewData.ModelState.IsValid)
{
    <div asp-validation-summary="All" class="alert alert-danger"></div>
}
*@
@if (TempData["Mensaje"] != null)
{
    <div class="alert alert-success">
        @TempData["Mensaje"]
    </div>
}
<form method="post" id="frmVenta" class="form">
    <div id="validationSummary"
         class="alert alert-danger"
         style="display:none">
    </div>
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">@Html.Raw(TempData["Error"])</div>
    }
    @Html.AntiForgeryToken()
    <fieldset class="border rounded p-3 mb-3">
        <legend class="float-none w-auto px-2 fs-6">
            <strong>Registro de la venta</strong>
        </legend>
        <div class="row mb-3">
            <div class="col-md-2">
                <label asp-for="VentaVM.OrderID" class="form-label">ID de la venta</label>
                <input asp-for="VentaVM.OrderID" class="form-control" readonly />
            </div>
            <div class="col-md-5">
                <label asp-for="VentaVM.CustomerID" class="form-label">Cliente</label>
                <select id="cboCliente" asp-for="VentaVM.CustomerID"
                        asp-items="Model.Clientes"
                        class="form-select" 
                        disabled="@Model.BloquearEdicion">
                </select>
                <span asp-validation-for="VentaVM.CustomerID" class="text-danger"></span>
            </div>
            <div class="col-md-5">
                <label asp-for="VentaVM.EmployeeID" class="form-label">Vendedor</label>
                <select id="cboVendedor" asp-for="VentaVM.EmployeeID"
                        asp-items="Model.Vendedores"
                        class="form-select"
                        disabled="@Model.BloquearEdicion">
                </select>
                <span asp-validation-for="VentaVM.EmployeeID" class="text-danger"></span>
            </div>
        </div>
        <div class="row mb-3">
            <div class="col-md-2">
                <label>Fecha de venta</label>
                <input asp-for="VentaVM.OrderDate" type="date" class="form-control" disabled="@Model.BloquearEdicion" />
            </div>
            <div class="col-md-2">
                <label>Hora</label>
                <input type="time"
                       id="VentaVM_OrderTime"
                       name="VentaVM.OrderTime"
                       value="@(Model.VentaVM.OrderTime?.ToString(@"hh\:mm"))"
                       step="60"
                       class="form-control" disabled="@Model.BloquearEdicion" />
            </div>
            <div class="col-md-2">
                <label>Fecha requerido</label>
                <input asp-for="VentaVM.RequiredDate" type="date" class="form-control" disabled="@Model.BloquearEdicion" />
            </div>
            <div class="col-md-2">
                <label>Hora</label>
                <input type="time"
                       id="VentaVM_RequiredTime"
                       name="VentaVM.RequiredTime"
                       value="@(Model.VentaVM.RequiredTime?.ToString(@"hh\:mm"))"
                       step="60"
                       class="form-control" disabled="@Model.BloquearEdicion" />
            </div>
            <div class="col-md-2">
                <label>Fecha de envío</label>
                <input asp-for="VentaVM.ShippedDate" type="date" class="form-control" disabled="@Model.BloquearEdicion" />
            </div>
            <div class="col-md-2">
                <label>Hora</label>
                <input type="time"
                       id="VentaVM_ShippedTime"
                       name="VentaVM.ShippedTime"
                       value="@(Model.VentaVM.ShippedTime?.ToString(@"hh\:mm"))"
                       step="60"
                       class="form-control" disabled="@Model.BloquearEdicion" />
            </div>
        </div>
        <fieldset class="border rounded p-3 mb-3">
            <legend class="float-none w-auto px-2 fs-6">
                <strong>Forma de envío:</strong>
            </legend>
            <div class="row mb-3">
                <div class="col-md-4">
                    <label asp-for="VentaVM.ShipVia" class="form-label">Compañía transportista</label>
                    <select id="cboCompañia" asp-for="VentaVM.ShipVia"
                            asp-items="Model.Transportistas"
                            class="form-select" disabled="@Model.BloquearEdicion">
                    </select>
                </div>
                <div class="col-md-4">
                    <label asp-for="VentaVM.ShipName" class="form-label">Enviar a</label>
                    <input asp-for="VentaVM.ShipName" class="form-control" disabled="@Model.BloquearEdicion" />
                </div>
                <div class="col-md-4">
                    <label asp-for="VentaVM.ShipAddress" class="form-label">Domicilio</label>
                    <input asp-for="VentaVM.ShipAddress" class="form-control" disabled="@Model.BloquearEdicion" />
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-md-4">
                    <label asp-for="VentaVM.ShipCity" class="form-label">Ciudad</label>
                    <input asp-for="VentaVM.ShipCity" class="form-control" disabled="@Model.BloquearEdicion" />
                </div>
                <div class="col-md-4">
                    <label asp-for="VentaVM.ShipRegion" class="form-label">Región</label>
                    <input asp-for="VentaVM.ShipRegion" class="form-control" disabled="@Model.BloquearEdicion" />
                </div>
                <div class="col-md-4">
                    <label asp-for="VentaVM.ShipPostalCode" class="form-label">Código postal</label>
                    <input asp-for="VentaVM.ShipPostalCode" class="form-control" disabled="@Model.BloquearEdicion" />
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-md-4">
                    <label asp-for="VentaVM.ShipCountry" class="form-label">País</label>
                    <input asp-for="VentaVM.ShipCountry" class="form-control" disabled="@Model.BloquearEdicion" />
                </div>
                <div class="col-md-4">
                    <label asp-for="VentaVM.Freight" class="form-label">Cargo por envío $</label>
                    <input asp-for="VentaVM.Freight"
                           class="form-control"
                           type="number"
                           step="1"
                           min="0"
                           disabled="@Model.BloquearEdicion" />
                </div>
                <div class="col-md-4 d-flex align-items-end">
                    <button type="button"
                            id="btnLimpiarEnvio"
                            class="btn btn-sm btn-warning"
                            title="Limpiar forma de envío"
                            disabled="@Model.BloquearEdicion">
                        <i class="bi bi-eraser"></i>
                        <span class="d-none d-md-inline">Limpiar forma de envío</span>
                    </button>
                    &nbsp;&nbsp;
                    <button type="button"
                            id="btnSeleccionarEnvio"
                            class="btn btn-sm btn-primary"
                            title="Seleccionar forma de envío"
                            disabled="@Model.BloquearEdicion">
                        <i class="bi bi-check-square"></i>
                        <span class="d-none d-md-inline">Seleccionar forma de envío</span>
                    </button>
                </div>
            </div>
        </fieldset>
        <fieldset class="border rounded p-3 mb-3">
            <legend class="float-none w-auto px-2 fs-6">
                <strong>Agregar producto a la venta:</strong>
            </legend>
            <div class="row mb-3">
                <div class="col-md-6">
                    <label asp-for="Detalle.CategoriaID" class="form-label">Categoría</label>
                    <select id="cboCategoria" asp-for="Detalle.CategoriaID"
                            asp-items="Model.Categorias"
                            class="form-select" disabled="@Model.BloquearEdicion">
                    </select>
                </div>
                <div class="col-md-6">
                    <label asp-for="Detalle.ProductID" class="form-label">Producto</label>
                    <select id="cboProducto" asp-for="Detalle.ProductID"
                            asp-items="Model.Productos"
                            class="form-select" disabled="@Model.BloquearEdicion">
                    </select>
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Detalle.UnitPrice" class="form-label flex-grow-1"><strong>Precio $</strong><br /><small class="text-muted">(con IVA incluido)</small></label>
                    <input asp-for="Detalle.UnitPrice" class="form-control" readonly />
                </div>
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Detalle.UnitsInStock" class="form-label flex-grow-1"><strong>Unidades en inventario</strong></label>
                    <input asp-for="Detalle.UnitsInStock" class="form-control" readonly />
                </div>
                <div class="col-md-2 d-flex flex-column">
                    <label asp-for="Detalle.Quantity" class="form-label flex-grow-1">Cantidad</label>
                    <input asp-for="Detalle.Quantity" class="form-control" disabled="@Model.BloquearEdicion" />
                </div>
                <div class="col-md-2 d-flex flex-column">
                    <label asp-for="Detalle.Discount" class="form-label flex-grow-1">Descuento (%)</label>
                    <input asp-for="Detalle.Discount"
                           class="form-control"
                           type="number"
                           step="1"
                           min="0"
                           max="95.00" disabled="@Model.BloquearEdicion" />
                </div>
                <div class="col-md-2 d-flex align-items-end">
                    <button type="button"
                            id="btnAgregarProducto"
                            class="btn btn-sm btn-primary"
                            title="Agregar"
                            disabled="@Model.BloquearEdicion">
                        <i class="bi bi-plus-lg"></i>
                        <span class="d-none d-md-inline"> Agregar</span>
                    </button>
                    <button type="button"
                            id="btnCancelarEdicion"
                            class="btn btn-sm btn-secondary ms-2 d-none"
                            title="Cancelar edición"
                            disabled="@Model.BloquearEdicion">
                        <i class="bi bi-x-circle"></i>
                        <span class="d-none d-md-inline"> Cancelar</span>
                    </button>
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Detalle.PrecioPorUnidadSinIVASinDescuento" class="form-label flex-grow-1">Precio p/u $<br /><small class="text-muted">(sin IVA incluido antes del descuento)</small></label>
                    <input asp-for="Detalle.PrecioPorUnidadSinIVASinDescuento" class="form-control" readonly />
                </div>
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Detalle.IVADelPrecioPorUnidadSinDescuento" class="form-label flex-grow-1">IVA incluido p/u $ <br /><small class="text-muted">(antes del descuento)</small></label>
                    <input asp-for="Detalle.IVADelPrecioPorUnidadSinDescuento" class="form-control" readonly />
                </div>
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Detalle.PrecioPorUnidadSinIVADespuesDescuento" class="form-label flex-grow-1">Precio p/u $ <br /><small class="text-muted">(sin IVA incluido despues del descuento)</small></label>
                    <input asp-for="Detalle.PrecioPorUnidadSinIVADespuesDescuento" class="form-control" readonly />
                </div>
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Detalle.IVADelPrecioporUnidadDespuesDescuento" class="form-label flex-grow-1">IVA p/u $ <br /><small class="text-muted">(despues del descuento)</small></label>
                    <input asp-for="Detalle.IVADelPrecioporUnidadDespuesDescuento" class="form-control" readonly />
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Detalle.AhorroPorUnidadSinIVA" class="form-label flex-grow-1">Ahorro p/u $ <br /><small class="text-muted">(Sin IVA despues del descuento)</small></label>
                    <input asp-for="Detalle.AhorroPorUnidadSinIVA" class="form-control" readonly />
                </div>
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Detalle.AhorroEnIVAPorUnidadDespuesDescuento" class="form-label flex-grow-1">Ahorro en IVA p/u $ <br /><small class="text-muted">(despues del descuento)</small></label>
                    <input asp-for="Detalle.AhorroEnIVAPorUnidadDespuesDescuento" class="form-control" readonly />
                </div>
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Detalle.AhorroTotalPorUnidadConIVA" class="form-label flex-grow-1">Ahorro total p/u $ <br /><small class="text-muted">(con IVA despues del descuento)</small></label>
                    <input asp-for="Detalle.AhorroTotalPorUnidadConIVA" class="form-control" readonly />
                </div>
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Detalle.PrecioPorUnidadConIVADespuesDescuento" class="form-label flex-grow-1">Precio p/u $ <br /><small class="text-muted">(con IVA incluido despues del descuento)</small></label>
                    <input asp-for="Detalle.PrecioPorUnidadConIVADespuesDescuento" class="form-control" readonly />
                </div>
            </div>
            <fieldset class="border rounded p-3 mb-3">
                <legend class="float-none w-auto px-2 fs-6">
                    <strong>Subtotales de la venta del producto:</strong>
                </legend>
                <div class="row mb-3">
                    <div class="col-md-3 d-flex flex-column">
                        <label asp-for="Detalle.SubtotalDelImporteConIVAIncluido" class="form-label flex-grow-1">Subtotal del importe $ <br /><small class="text-muted">(Precio con IVA incluido)</small></label>
                        <input asp-for="Detalle.SubtotalDelImporteConIVAIncluido" class="form-control" readonly />
                    </div>
                    <div class="col-md-3 d-flex flex-column">
                        <label asp-for="Detalle.SubtotalDelImporteSinIVASinDescuento" class="form-label flex-grow-1">Subtotal del importe sin IVA antes del descuento $ <br /><small class="text-muted">(Precio sin IVA incluido antes del descuento)</small></label>
                        <input asp-for="Detalle.SubtotalDelImporteSinIVASinDescuento" class="form-control" readonly />
                    </div>
                    <div class="col-md-3 d-flex flex-column">
                        <label asp-for="Detalle.SubtotalDelImporteDelIVASinDescuento" class="form-label flex-grow-1">Subtotal del IVA incluido $ <br /><small class="text-muted">(Antes del descuento)</small></label>
                        <input asp-for="Detalle.SubtotalDelImporteDelIVASinDescuento" class="form-control" readonly />
                    </div>
                    <div class="col-md-3 d-flex flex-column">
                        <label asp-for="Detalle.SubtotalDelImporteSinIVAConDescuento" class="form-label flex-grow-1">Subtotal del importe sin IVA despues del descuento $ <br /><small class="text-muted">(Precio sin IVA incluido despues del descuento)</small></label>
                        <input asp-for="Detalle.SubtotalDelImporteSinIVAConDescuento" class="form-control" readonly />
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col-md-3 d-flex flex-column">
                        <label asp-for="Detalle.SubtotalIVADespuesDelDescuento" class="form-label flex-grow-1">Subtotal del IVA despues del descuento $</label>
                        <input asp-for="Detalle.SubtotalIVADespuesDelDescuento" class="form-control" readonly />
                    </div>
                    <div class="col-md-3 d-flex flex-column">
                        <label asp-for="Detalle.SubtotalDelAhorroSinIvaDespuesDescuento" class="form-label flex-grow-1">Subtotal del ahorro sin IVA $ <br /><small class="text-muted">(despues del descuento)</small></label>
                        <input asp-for="Detalle.SubtotalDelAhorroSinIvaDespuesDescuento" class="form-control" readonly />
                    </div>
                    <div class="col-md-3 d-flex flex-column">
                        <label asp-for="Detalle.SubtotalDelAhorroEnIVADespuesDescuento" class="form-label flex-grow-1">Subtotal del ahorro en IVA $ <br /><small class="text-muted">(despues del descuento)</small></label>
                        <input asp-for="Detalle.SubtotalDelAhorroEnIVADespuesDescuento" class="form-control" readonly />
                    </div>
                    <div class="col-md-3 d-flex flex-column">
                        <label asp-for="Detalle.SubtotalDelAhorroTotalDespuesDescuento" class="form-label flex-grow-1">Subtotal del ahorro total $ <br /><small class="text-muted">(despues del descuento)</small></label>
                        <input asp-for="Detalle.SubtotalDelAhorroTotalDespuesDescuento" class="form-control" readonly />
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col-md-3 d-flex flex-column">
                    </div>
                    <div class="col-md-3 d-flex flex-column">
                    </div>
                    <div class="col-md-3 d-flex flex-column">
                    </div>
                    <div class="col-md-3 d-flex flex-column">
                        <label asp-for="Detalle.Subtotal" class="form-label flex-grow-1"><strong>Total del producto $</strong></label>
                        <input asp-for="Detalle.Subtotal" class="form-control" readonly />
                    </div>
                </div>
            </fieldset>
        </fieldset>
        <fieldset class="border rounded p-3 mb-3">
            <legend class="float-none w-auto px-2 fs-6">
                <strong>Detalle de la venta:</strong>
            </legend>
            <div class="table-responsive">
                <table class="table table-bordered table-hover table-striped align-middle" style="font-size:0.75rem;">
                    <thead class="table-light">
                        <tr>
                            <th class="text-center">N°</th>
                            <th class="text-center">Producto</th>
                            <th class="text-center">Precio <br /><small class="text-muted">(con IVA incluido)</small></th>
                            <th class="text-center">Cantidad</th>
                            <th class="text-center">Importe</th>
                            <th class="text-center">Desc.</th>
                            <th class="text-center">Importe del descuento</th>
                            <th class="text-center">Importe con descuento</th>
                            <th class="text-center">Tasa IVA</th>
                            <th class="text-center">Importe sin IVA</th>
                            <th class="text-center">Importe del IVA <br /><small class="text-muted">(incluido)</small></th>
                            <th class="text-center">Subtotal</th>
                            <th class="text-center" width="120">Acciones</th>
                            <th class="d-none"></th>
                        </tr>
                    </thead>
                    <tbody id="tblDetalle">
                    </tbody>
                </table>
            </div>
        </fieldset>
        <fieldset class="border rounded p-3 mb-3">
            <legend class="float-none w-auto px-2 fs-6">
                <strong>Totales de la venta:</strong>
            </legend>
            <div class="row mb-3">
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Totales.NumeroProductos" class="form-label flex-grow-1">Número de productos</label>
                    <input asp-for="Totales.NumeroProductos" class="form-control" readonly />
                </div>
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Totales.TotalUnidades" class="form-label flex-grow-1">Total de unidades</label>
                    <input asp-for="Totales.TotalUnidades" class="form-control" readonly />
                </div>
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Totales.TotalImporteConIVA" class="form-label flex-grow-1">Subtotal del importe $ <br /><small class="text-muted">(precio con IVA incluido)</small></label>
                    <input asp-for="Totales.TotalImporteConIVA" class="form-control" readonly />
                </div>
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Totales.TotalDescuento" class="form-label flex-grow-1">Subtotal del importe del descuento $</label>
                    <input asp-for="Totales.TotalDescuento" class="form-control" readonly />
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Totales.TotalImporteConDescuento" class="form-label flex-grow-1">Subtotal del importe con descuento $</label>
                    <input asp-for="Totales.TotalImporteConDescuento" class="form-control" readonly />
                </div>
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Totales.TotalImporteSinIVA" class="form-label flex-grow-1">Subtotal del importe sin IVA $</label>
                    <input asp-for="Totales.TotalImporteSinIVA" class="form-control" readonly />
                </div>
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Totales.TotalIVA" class="form-label flex-grow-1">Subtotal del importe del IVA $ <br /><small class="text-muted">(incluido)</small></label>
                    <input asp-for="Totales.TotalIVA" class="form-control" readonly />
                </div>
                <div class="col-md-3 d-flex flex-column">
                    <label asp-for="Totales.Total" class="form-label flex-grow-1"><strong>Total</strong></label>
                    <input asp-for="Totales.Total" class="form-control" readonly />
                </div>
            </div>
        </fieldset>
        <div class="d-flex justify-content-end gap-2 mb-3">
            <a asp-page="./Insertar"
               id="btnNuevaVenta"
               class="btn btn-sm btn-primary"
               title="Nueva venta">
                <i class="bi bi-file-earmark-plus"></i>
                <span class="d-none d-md-inline"> Nueva venta</span>
            </a>
            <button type="button"
                    id="btnNotaRemision"
                    class="btn btn-sm btn-warning"
                    title="Nota de remisión"
                    disabled="@(!Model.VentaGuardada)"
                    data-url="@Url.Page(
                                                    "/Ventas/Reportes/NotaRemisionRpt",
                                                    pageHandler: "Pdf",
                                                    values: new { orderId = Model.OrderID })">
                <i class="bi bi-printer"></i>
                <span class="d-none d-md-inline">Nota de remisión</span>
            </button>
            <a href="@(Model.ReturnUrl ?? Url.Page("Index"))"
               class="btn btn-sm btn-cancelar"
               title="Cancelar">
                <i class="bi bi-x-circle"></i>
                <span class="d-none d-md-inline"> Cancelar</span>
            </a>
        </div>
    </fieldset>
    <input asp-for="OrderID" type="hidden" />
    <input asp-for="VentaVM.RowVersion" type="hidden" />
</form>
<div class="modal fade"
     id="modalFormasEnvio"
     tabindex="-1"
     aria-labelledby="modalFormasEnvioLabel"
     aria-hidden="true">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modalFormasEnvioLabel">
                    Seleccionar forma de envío
                </h5>
                <button type="button"
                        class="btn-close"
                        data-bs-dismiss="modal">
                </button>
            </div>
            <div class="modal-body">
                <div class="table-responsive">
                    <table id="tblFormasEnvio"
                           class="table table-bordered table-hover table-striped align-middle"
                           style="font-size:0.65rem;">
                        <thead class="table-light">
                            <tr>
                                <th class="text-center">Compañía transportista</th>
                                <th class="text-center">Enviar a</th>
                                <th class="text-center">Domicilio</th>
                                <th class="text-center">Ciudad</th>
                                <th class="text-center">Región</th>
                                <th class="text-center">Código postal</th>
                                <th class="text-center">País</th>
                                <th class="d-none"></th>
                                <th class="text-center" width="100">Acción</th>
                            </tr>
                        </thead>
                        <tbody id="tbodyFormasEnvio">
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button"
                        class="btn btn-secondary"
                        data-bs-dismiss="modal">
                    Cerrar
                </button>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    <script>
        function mostrarErrorValidacion(mensaje) {
            const summary = $("#validationSummary");
            summary
                .html("<ul><li>" + mensaje + "</li></ul>")
                .show();
        }

        function limpiarErrorValidacion() {
            const summary = $("#validationSummary");
            summary
                .html("")
                .hide();
        }

        function bloquearEdicionPorConcurrencia() {

            // Encabezado de la venta
            $("#cboCliente").prop("disabled", true);
            $("#cboVendedor").prop("disabled", true);

            $("#VentaVM_OrderDate").prop("disabled", true);
            $("#VentaVM_OrderTime").prop("disabled", true);

            $("#VentaVM_RequiredDate").prop("disabled", true);
            $("#VentaVM_RequiredTime").prop("disabled", true);

            $("#VentaVM_ShippedDate").prop("disabled", true);
            $("#VentaVM_ShippedTime").prop("disabled", true);

            // Forma de envío
            $("#cboCompañia").prop("disabled", true);

            $("#VentaVM_ShipName").prop("disabled", true);
            $("#VentaVM_ShipAddress").prop("disabled", true);
            $("#VentaVM_ShipCity").prop("disabled", true);
            $("#VentaVM_ShipRegion").prop("disabled", true);
            $("#VentaVM_ShipPostalCode").prop("disabled", true);
            $("#VentaVM_ShipCountry").prop("disabled", true);
            $("#VentaVM_Freight").prop("disabled", true);

            $("#btnLimpiarEnvio").prop("disabled", true);
            $("#btnSeleccionarEnvio").prop("disabled", true);

            // Captura de producto
            $("#cboCategoria").prop("disabled", true);
            $("#cboProducto").prop("disabled", true);

            $("#Detalle_Quantity").prop("disabled", true);
            $("#Detalle_Discount").prop("disabled", true);

            $("#btnAgregarProducto").prop("disabled", true);
            $("#btnCancelarEdicion").prop("disabled", true);

            // Detalles existentes
            $(".btnEditar").prop("disabled", true);
            $(".btnEliminar").prop("disabled", true);
        }

        function actualizarEncabezado() {
            const request = {
                OrderID: parseInt($("#VentaVM_OrderID").val()),
                CustomerID: $("#cboCliente").val(),
                EmployeeID: parseInt($("#cboVendedor").val()),
                OrderDate: $("#VentaVM_OrderDate").val() || null,
                OrderTime: $("#VentaVM_OrderTime").val() || null,
                RequiredDate: $("#VentaVM_RequiredDate").val() || null,
                RequiredTime: $("#VentaVM_RequiredTime").val() || null,
                ShippedDate: $("#VentaVM_ShippedDate").val() || null,
                ShippedTime: $("#VentaVM_ShippedTime").val() || null,
                RowVersion: $("#VentaVM_RowVersion").val()
            };
            $.ajax({
                url: "?handler=ActualizarEncabezado",
                type: "POST",
                contentType: "application/json",
                headers: {
                    "RequestVerificationToken":
                        $('input[name="__RequestVerificationToken"]').val()
                },
                data: JSON.stringify(request),
                success: function(response) {
                    if (response.ok) {
                        limpiarErrorValidacion();
                        //Actualizar la nueva RowVersion
                        $("#VentaVM_RowVersion").val(response.rowVersion);
                    } else {
                        mostrarErrorValidacion(response.mensaje);
                        if (
                            response.codigo === -1 ||
                            response.codigo === -2 ||
                            response.codigo === -99
                        ) {
                            bloquearEdicionPorConcurrencia();
                        }
                    }
                },
                error: function(xhr) {
                    mostrarErrorValidacion("Error al actualizar el encabezado.");
                    console.error(xhr);
                }
            });
        }

        function llenarTabla(lista) {
            // console.log("LISTA RECIBIDA:", lista);
            let html = "";
            lista.forEach(function(item, indice) {
                html += `
                                    <tr>
                                        <td class="text-center">${indice + 1}</td>
                                        <td>${item.productName}</td>
                                        <td class="text-center">
                                            ${Number(item.unitPrice).toLocaleString('es-MX',
                    { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
                                        </td>
                                        <td class="text-center">
                                            ${item.quantity}
                                        </td>
                                        <td class="text-end">
                                            ${Number(item.subtotalDelImporteConIVAIncluido).toLocaleString('es-MX',
                        { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
                                        </td>
                                        <td class="text-center">
                                            <nobr>
                                                ${(item.discount * 100).toFixed(2)} %
                                            </nobr>
                                        </td>
                                        <td class="text-end">
                                            ${Number(item.subtotalDelAhorroTotalDespuesDescuento).toLocaleString('es-MX',
                            { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
                                        </td>
                                        <td class="text-end">
                                            ${Number(item.subtotalDelImporteConIVAConDescuento).toLocaleString('es-MX',
                                { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
                                        </td>
                                        <td class="text-center">
                                            <nobr>
                                                ${(item.tasaIVA * 100).toFixed(2)} %
                                            </nobr>
                                        </td>
                                        <td class="text-end">
                                            ${Number(item.subtotalDelImporteSinIVAConDescuento).toLocaleString('es-MX',
                                    { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
                                        </td>
                                        <td class="text-end">
                                            ${Number(item.subtotalIVADespuesDelDescuento).toLocaleString('es-MX',
                                        { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
                                        </td>
                                        <td class="text-end fw-bold">
                                            ${Number(item.subtotal).toLocaleString('es-MX',
                                            { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
                                        </td>
                                        <td class="text-center">
                                            <button type="button"
                                                    class="btn btn-sm btn-warning btnEditar"
                                                    data-product-id="${item.productID}"
                                                    data-row-version="${item.rowVersion}">
                                                <i class="bi bi-pencil"></i>
                                            </button>
                                            <button type="button"
                                                    class="btn btn-sm btn-danger btnEliminar"
                                                    data-index="${indice}"
                                                    data-product-id="${item.productID}"
                                                    data-row-version="${item.rowVersion}">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </td>
                                        <td class="d-none">
                                            ${item.productID}
                                        </td>
                                    </tr>`;
            });
            $("#tblDetalle").html(html);
        }

        function actualizarEnvio() {
            const request = {
                OrderID: parseInt($("#VentaVM_OrderID").val()),
                ShipName: $("#VentaVM_ShipName").val(),
                ShipAddress: $("#VentaVM_ShipAddress").val(),
                ShipCity: $("#VentaVM_ShipCity").val(),
                ShipRegion: $("#VentaVM_ShipRegion").val(),
                ShipPostalCode: $("#VentaVM_ShipPostalCode").val(),
                ShipCountry: $("#VentaVM_ShipCountry").val(),
                ShipVia: $("#cboCompañia").val() || null,
                Freight: $("#VentaVM_Freight").val() || null,
                RowVersion: $("#VentaVM_RowVersion").val()
            };
            $.ajax({
                url: "?handler=ActualizarEnvio",
                type: "POST",
                contentType: "application/json",
                headers: {
                    "RequestVerificationToken":
                        $('input[name="__RequestVerificationToken"]').val()
                },
                data: JSON.stringify(request),
                success: function(response) {
                    if (response.ok) {
                        limpiarErrorValidacion();
                        $("#VentaVM_RowVersion").val(response.rowVersion);
                    }
                    else {
                        mostrarErrorValidacion(response.mensaje);
                        if (
                            response.codigo === -1 ||
                            response.codigo === -2 ||
                            response.codigo === -99
                        ) {
                            bloquearEdicionPorConcurrencia();
                        }
                    }
                },
                error: function(xhr) {
                    mostrarErrorValidacion("Error al actualizar el encabezado.");
                    console.error(xhr);
                }
            });
        }

        $(
            "#cboCliente," +
            "#cboVendedor," +
            "#VentaVM_OrderDate," +
            "#VentaVM_OrderTime," +
            "#VentaVM_RequiredDate," +
            "#VentaVM_RequiredTime," +
            "#VentaVM_ShippedDate," +
            "#VentaVM_ShippedTime"
        ).on("change", actualizarEncabezado);

        $(
            "#VentaVM_ShipName," +
            "#VentaVM_ShipAddress," +
            "#VentaVM_ShipCity," +
            "#VentaVM_ShipRegion," +
            "#VentaVM_ShipPostalCode," +
            "#VentaVM_ShipCountry," +
            "#cboCompañia," +
            "#VentaVM_Freight"
        ).on("change", actualizarEnvio);

        var detalleVenta = @Html.Raw(Model.DetallesJson);
        if (detalleVenta.length > 0) {
            llenarTabla(detalleVenta);
        }
        mostrarTotales(@Html.Raw(Model.TotalesJson));

    </script>
}